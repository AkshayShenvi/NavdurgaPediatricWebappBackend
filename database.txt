CREATE table employee (name varchar(10) NOT NULL,
                      salary int,
                      city varchar(10),
                      ID int PRIMARY KEY);

ALTER TABLE employees ADD department varchar(20);

ALTER TABLE employees ADD joining_date date;

ALTER TABLE employees MODIFY department varchar(20) NOT NULL;

INSERT INTO employee (name,salary,city,department,ID,joining_date) VALUES ('Akshay',25000,'Mumbai','CS',1001670648,2001-18-09);


Select name from employee where salary>(SELECT AVG(salary)from employee);

Select name from employee GROUP BY department having Count(department)= 1;

Select name from employee where salary = (select max(salary) from employee);

INSERT INTO employees_backup
SELECT * 
FROM employee
WHERE department='CS';

